# 19a - Playroom Bug Fixes

I created a free-plan account with Playroom and created an application, which gives me a free tier game id.
I provided the VITE_PLAYROOM_GAME_ID before my run command and attempted to start a game between 2 different sessions.
I have found at least one bug which unfortunately prevents further testing of the playroom integration,
because it never closes the "waiting for players" popup.

I create a game, copy the link, go to that address on a 2nd device, and I see a message "2 of 2 players seated"
But the first device never displays a message saying the 2nd person joined. And Both devices don't proceed past the "table ready" popup window
for when users are still joining.

## Goals

- Fix the popup bug (my 2nd ui says 2 of 2 players seated. The first UI says "1 of 2", so it didn't get the message ")
- Weigh in on the complexity of building a local version of the playroom service that works for testing.

## Acceptance Criteria

- the integration with playroom correctly accepts and broadcasts the 2nd player joining the game.
- Both clients respond to the 2nd player joining.
- The "table ready" popup window closes when 2 players join and the game.

## Usage

Under 1 percent on "light", yielded an incorrect result. Trying medium.
1 percent on "medium" - succeeded
